# Optional - only needed if you want to override the defaults (e.g. switch to a
# cloud OpenAI-compatible provider later). Local vLLM works with no .env file at all.
# docker compose automatically reads .env in this directory.

# OPENAI_BASE_URL=http://192.168.1.251:8000
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=qwen/qwen3.5

# DB_HOST=host.docker.internal
# DB_PORT=5432
