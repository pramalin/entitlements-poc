<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.0</version>
    <relativePath/>
  </parent>

  <groupId>com.alai</groupId>
  <artifactId>entitlements-llm-utility</artifactId>
  <version>0.1.0</version>
  <name>entitlements-llm-utility</name>
  <description>
    Offline, run-once batch job. Reads entitlements with no description yet,
    asks Claude (via Spring AI) to write one, writes it to entitlement_descriptions.
    Deliberately NOT a long-running service - no web starter, exits the JVM when done.
  </description>

  <properties>
    <java.version>25</java.version>
    <!-- Spring AI 2.0.0 is the line built for Spring Boot 4.0/4.1 + Spring Framework 7,
         which is exactly what backend/pom.xml uses - confirmed via Spring's own 2.0.0
         GA announcement before picking this, rather than guessing given how much the
         Boot 4 modularization already cost us in backend/. -->
    <spring-ai.version>2.0.0</spring-ai.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jdbc</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.ai</groupId>
      <artifactId>spring-ai-starter-model-openai</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>
  </dependencies>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-bom</artifactId>
        <version>${spring-ai.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <finalName>entitlements-llm-utility</finalName>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
